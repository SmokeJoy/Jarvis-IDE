import React from 'react';

// Placeholder per il grafico di trend
// Richiederebbe dati storici da più report
const TrendChart: React.FC = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-gray-400 italic">Trend Chart (Implementazione futura)</p>
    </div>
  );
};

export default TrendChart; 